import { BrToNewlinePipe } from './br-to-newline.pipe';

describe('BrToNewlinePipe', () => {
  it('create an instance', () => {
    const pipe = new BrToNewlinePipe();
    expect(pipe).toBeTruthy();
  });
});
