import { FilterPhoneNoPipe } from './filter-phone-no.pipe';

describe('FilterPhoneNoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPhoneNoPipe();
    expect(pipe).toBeTruthy();
  });
});
