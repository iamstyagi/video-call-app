import { HtmlconverterPipe } from './htmlconverter.pipe';

describe('HtmlconverterPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlconverterPipe();
    expect(pipe).toBeTruthy();
  });
});
