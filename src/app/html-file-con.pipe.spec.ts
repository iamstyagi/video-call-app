import { HtmlFileConPipe } from './html-file-con.pipe';

describe('HtmlFileConPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlFileConPipe();
    expect(pipe).toBeTruthy();
  });
});
